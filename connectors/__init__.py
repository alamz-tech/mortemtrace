"""Vendor-agnostic inbound ingestion.

One receiver, configured by data, rather than one adapter per vendor.
See data/models.py's connector section for why that choice was made and
where its limits are.
"""
